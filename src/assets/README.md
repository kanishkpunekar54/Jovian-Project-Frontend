# Assets

The `assets` folder contains static files that are publicly accessible.

## Purpose
- Store images, fonts, icons, and other static resources
- Used in stylesheets or templates

## Examples
- `images/`
- `icons/`
- `translations/` (for i18n)

> 📁 Files in this folder are served as-is and can be accessed via `/assets/` path.
